# Core + dev dependencies. `pip install -e ".[dev,separation]"` is the preferred
# route — pyproject.toml is the source of truth for the optional extras.
numpy>=1.24
scipy>=1.11
librosa>=0.10
soundfile>=0.12
matplotlib>=3.7
pesq>=0.0.4
pystoi>=0.3.3
pytest>=7.4
jupyter>=1.0

# N-speaker research track (extra: separation). On Blackwell GPUs (RTX 50xx,
# sm_120) install torch from the cu128 nightly index instead — stable wheels
# do not ship sm_120 kernels.
speechbrain>=1.1
torch>=2.1
torchaudio>=2.1
